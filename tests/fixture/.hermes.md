# Fixture project — Phase 1 testing

Small Python project used to test the agent workflows.

## Build & Test
- Test command: `python3 -m pytest -q`
- Gate runner: `bash ../bin/gates.sh`

## Conventions
- Type hints on all functions
- Docstrings on all public functions
- Tests must pass before any commit
